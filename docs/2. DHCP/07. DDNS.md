# 7. DDNS

## Objetivos
- Configurar actualizaciones dinámicas entre DHCP y DNS.
- Diferenciar actualizaciones seguras e inseguras.

## 7.1 Tipos de DDNS
- **none**
- **ad-hoc**
- **interim (recomendado)**

## 7.2 Configuración insegura (sin TSIG)
Ejemplo en BIND9:

```
allow-update { any; };
```

## 7.3 Configuración segura (con TSIG)

Clave TSIG:
```
key "dhcpupdate" {
    algorithm hmac-sha256;
    secret "CLAVE==";
};
```

Definición de zonas:
```
zone "example.com" {
    allow-update { key dhcpupdate; };
}
```
